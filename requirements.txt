streamlit==1.32.0  # Consider upgrading to the latest version
joblib==1.3.2
emoji==2.10.0  # Upgrade for latest features
nltk==3.8.1
pandas==2.1.1
scikit-learn==1.3.0
python-dotenv==1.0.0
requests==2.31.0
gdown==4.7.1
numpy==1.26.0
tqdm==4.66.1  # Keep if used in your code
loguru==0.7.0  # Keep if used for logging
setuptools>=58.0.0  # Specify a version for compatibility
pillow==9.4.0  # Keep if used for image processing
